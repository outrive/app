# OUTRIVE Autonomous Agent — Skill Configuration

> This file defines the trading strategy for your OUTRIVE Autonomous Agent Vault.
> Upload or paste this into the Strategy panel, or reference it via API.

---

## Agent Identity

```yaml
agent_version: "1.0"
created_by: "YOUR_WALLET_ADDRESS"
network: "robinhood-chain"
```

---

## Target Asset

```yaml
token: "AAPL"          # One of: AAPL NVDA TSLA GOOGL META MSFT AMZN AMD PLTR ORCL SPY MU SPCX
strategy: "momentum"   # dca | momentum | dip-buy | breakout | custom
```

---

## Entry Condition

```yaml
entry:
  type: "dip"          # market | limit | dip | breakout
  # market  → execute at next market open immediately
  # limit   → execute when price ≤ limit_price
  # dip     → execute when price drops dip_pct% below current
  # breakout → execute when price rises above recent N-day high

  limit_price: null    # USD — used when type = "limit"
  dip_pct: 3.0         # % drop required — used when type = "dip"
  breakout_days: 20    # lookback window — used when type = "breakout"
```

---

## Risk Parameters

```yaml
risk:
  tp_pct: 15.0         # Take Profit — close position when P&L ≥ +15%
  sl_pct: 5.0          # Stop Loss   — close position when P&L ≤ -5%
  trailing_sl: false   # If true, SL trails price upward as position gains
```

---

## Position Sizing

```yaml
position:
  budget_eth: 0.05     # ETH to spend per trade entry
  max_concurrent: 3    # Max open positions at the same time
  cooldown_hours: 6    # Min hours between two entries on the same token
```

---

## Execution

```yaml
execution:
  check_interval_min: 30   # How often the agent checks conditions (minutes)
  dry_run: false           # If true, log actions but do not submit on-chain tx
  max_slippage_bps: 50     # Max acceptable slippage in basis points (50 = 0.5%)
```

---

## Example: Aggressive NVDA Momentum

```yaml
token: "NVDA"
strategy: "momentum"
entry:
  type: "breakout"
  breakout_days: 10
risk:
  tp_pct: 30.0
  sl_pct: 8.0
  trailing_sl: true
position:
  budget_eth: 0.1
  max_concurrent: 2
  cooldown_hours: 12
execution:
  check_interval_min: 15
  dry_run: false
  max_slippage_bps: 75
```

---

## API Usage

```bash
# Apply this skill via API
curl -X POST https://api.outrive.io/autonomous/vault \
  -H "Authorization: Bearer OTR-your-key-here" \
  -H "Content-Type: application/json" \
  -d @agent-skill-template.json
```

---

*Generated by OUTRIVE · outrive.io*
